<Project Sdk="Microsoft.NET.Sdk">

  <!--
    The domain and the policy: Note, the INoteRepository contract, the services
    that hold rules, and UserPaths.

    It references nothing but the BCL, and that is the point rather than an
    accident - Core is what the fast tests run against, so anything dragged in
    here is dragged into every one of them. CoreReferencesOnlyTheBclTests guards it.
  -->
  <ItemGroup>
    <InternalsVisibleTo Include="SmartNotes.Core.Tests" />
  </ItemGroup>

</Project>
